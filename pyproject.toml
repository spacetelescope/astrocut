[build-system]
requires = ["setuptools",
            "setuptools_scm",
            "wheel"]
build-backend = 'setuptools.build_meta'

[tool.ruff]
line-length = 120

[tool.ruff.lint]
ignore = ["E303", "E266", "E226", "E203"]
